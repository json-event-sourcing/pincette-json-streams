---
application: s3csv-resumable
version: "1.0"
parts:
  - type: stream
    name: stream
    fromTopic: in
    toTopic: out
    pipeline:
      - $s3Csv:
          bucket: "$bucket"
          key: "$key"
          eTag: "$eTag"
          separator: "\t"
